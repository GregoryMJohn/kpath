# kpath
A simpe terminal user interface (TUI) to select custom k-point paths for electronic and phonon band structure calculations, with the ability to interpolate a user-specified number of k-points along the path.
